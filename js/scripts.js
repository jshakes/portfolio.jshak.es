var initRoyalSlider = (function(){

  var args = {
    keyboardNavEnabled: true,
    autoScaleSlider: false,
    autoScaleSliderWidth: 800,
    controlNavigation: 'tabs'
  }

  $(".slideshow").royalSlider(args);
})();